To run the application is necessary run the "SoftwareDesign.View" and "SoftwareDesign.ControllerLayer.API".
To do this, right click in the solution and select Properties.
Set the ConfigurationManager as the image in this folder.